/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.scdn.co','encrypted-tbn0.gstatic.com', 'img.icons8.com'],
  },
}

module.exports = nextConfig
